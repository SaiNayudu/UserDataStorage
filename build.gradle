plugins {
    id 'java'
    id 'application'
}

group 'com.example'
version '1.0'
sourceCompatibility = '17'

application {
    mainClass = 'com.example.userdatastore.Main'
}

repositories {
    mavenCentral()
}

dependencies {
    // no external dependencies; using standard Java
}
